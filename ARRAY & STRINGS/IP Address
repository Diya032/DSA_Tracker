string defangIPaddr(string address) {
    string s = "[.]";
        
    for(int i=0; i<address.size(); i++) {
        if(address[i] == '.') {
            address.replace(i, 1, s);
            i = i+2;
        }
    }
    return address;
}

// python

x = string.replace(".","[.]")
return x
