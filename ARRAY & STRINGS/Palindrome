// C++ Solution

class Solution{
public:

Bool isPalindrome(int x){
    long long int num = x, sum =0, digit;
    for(int i = x; i>0;i=i/10){
        digit = i%10;
        sum=sum*10+digit;    
    }
    
    if(num==sum) return true;
    else{
        return false;
        }
        
  }
};
